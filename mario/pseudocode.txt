1. prompt user for height
2. check if value between 1 and 8, inclusive
    if not return to 1.
3. print height number of rows:
    a. in each row print X (height - 1 - row number) number of spaces
    b. followed by Y (row number + 1) number of hashes
    c. print new line
